import React from "react";
import { AuthContainer, LoginForm } from "../components";

function LoginPage() {
  return (
    <AuthContainer>
      <LoginForm  />
    </AuthContainer>
  );
}

export default LoginPage;
